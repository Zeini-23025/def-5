# 🎮 نظام التلعيب (Gamification System)

نظام تلعيب متكامل مع معايير WCAG للوصول، بني باستخدام React و Express و MongoDB

---

## ✨ المميزات

### 🔐 نظام المصادقة
- تسجيل دخول آمن بـ JWT
- التحقق من صلاحيات المدير
- حماية المسارات (Protected Routes)

### 👥 إدارة الفرق
- إضافة وتعديل وحذف الفرق
- إضافة نقاط للفرق
- عرض عدد الأعضاء لكل فريق
- تحديث تلقائي كل 10 ثواني

### 🏆 لوحة المتصدرين
- ترتيب الفرق حسب النقاط
- ميداليات للمراكز الثلاثة الأولى
- تحديث فوري عند إضافة النقاط

### 📊 الإحصائيات
- إجمالي الفرق
- إجمالي الأعضاء
- إجمالي النقاط
- متوسط النقاط

### ♿ معايير الوصول (WCAG)
- ألوان عالية التباين
- دعم قارئات الشاشة
- التنقل بلوحة المفاتيح
- Responsive Design
- تقليل الحركة للمستخدمين الذين يفضلون ذلك

---

## 🛠️ التقنيات المستخدمة

### Backend
- **Node.js** - بيئة التشغيل
- **Express.js** - إطار عمل الويب
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ODM لـ MongoDB
- **JWT** - المصادقة
- **bcryptjs** - تشفير كلمات المرور

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **Axios** - طلبات HTTP
- **Lucide React** - الأيقونات
- **CSS3** - التصميم

---

## 📁 هيكل المشروع

```
gamification-system/
├── backend/
│   ├── server.js           # السيرفر الرئيسي
│   ├── package.json
│   ├── .env
│   └── node_modules/
│
└── frontend/
    ├── public/
    │   └── index.html
    ├── src/
    │   ├── components/
    │   │   ├── Auth/
    │   │   │   └── LoginPage.jsx
    │   │   ├── Dashboard/
    │   │   │   ├── DashboardHeader.jsx
    │   │   │   ├── StatsCards.jsx
    │   │   │   └── Leaderboard.jsx
    │   │   ├── Teams/
    │   │   │   ├── TeamsList.jsx
    │   │   │   ├── TeamCard.jsx
    │   │   │   └── TeamModal.jsx
    │   │   └── common/
    │   │       ├── Button.jsx
    │   │       └── Modal.jsx
    │   ├── services/
    │   │   └── api.js
    │   ├── styles/
    │   │   ├── global.css
    │   │   ├── dashboard.css
    │   │   ├── teams.css
    │   │   └── login.css
    │   ├── utils/
    │   │   └── auth.js
    │   ├── App.jsx
    │   └── index.js
    ├── package.json
    └── .env
```

---

## 🚀 التثبيت والتشغيل

### المتطلبات الأساسية
- Node.js (v16 أو أحدث)
- MongoDB (v5 أو أحدث)
- npm أو yarn

### 1. تثبيت MongoDB

#### على Windows:
```bash
# قم بتحميل MongoDB من الموقع الرسمي
https://www.mongodb.com/try/download/community

# بعد التثبيت، قم بتشغيل MongoDB
mongod
```

#### على macOS:
```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

#### على Linux:
```bash
sudo apt-get install -y mongodb
sudo systemctl start mongodb
sudo systemctl enable mongodb
```

### 2. إعداد Backend

```bash
# انتقل إلى مجلد backend
cd backend

# تثبيت المكتبات
npm install

# إنشاء ملف .env
cat > .env << EOL
MONGODB_URI=mongodb://localhost:27017/gamification
JWT_SECRET=gamification_secret_key_2024_ultra_secure
PORT=5000
FRONTEND_URL=http://localhost:3000
EOL

# تشغيل السيرفر
npm start

# أو للتطوير مع التحديث التلقائي
npm run dev
```

### 3. إنشاء حساب المدير (أول مرة فقط)

افتح ملف `server.js` وقم بإلغاء التعليق عن السطر الأخير:

```javascript
// من:
// createAdminUser();

// إلى:
createAdminUser();
```

ثم قم بتشغيل السيرفر مرة واحدة لإنشاء حساب المدير، ثم أعد التعليق على السطر مرة أخرى.

**بيانات المدير الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

### 4. إعداد Frontend

```bash
# افتح نافذة terminal جديدة
cd frontend

# تثبيت المكتبات
npm install

# إنشاء ملف .env
echo "REACT_APP_API_URL=http://localhost:5000/api" > .env

# تشغيل التطبيق
npm start
```

---

## 🎯 كيفية الاستخدام

### 1. تسجيل الدخول
- افتح المتصفح على `http://localhost:3000`
- استخدم بيانات المدير:
  - اسم المستخدم: `admin`
  - كلمة المرور: `admin123`

### 2. إضافة فريق جديد
- انقر على زر "إضافة فريق"
- أدخل اسم الفريق وعدد الأعضاء
- انقر "إضافة"

### 3. إضافة نقاط لفريق
- في بطاقة الفريق، أدخل عدد النقاط
- انقر على زر "إضافة"
- سيتم تحديث لوحة المتصدرين تلقائياً

### 4. تعديل فريق
- انقر على أيقونة القلم (✏️)
- عدّل البيانات
- انقر "حفظ"

### 5. حذف فريق
- انقر على أيقونة سلة المهملات (🗑️)
- أكّد الحذف

### 6. إضافة مستخدم جديد
- انقر على زر "إضافة مستخدم"
- أدخل البيانات المطلوبة
- حدد صلاحيات المدير إذا لزم الأمر
- انقر "إضافة"

---

## 📡 API Endpoints

### المصادقة (Authentication)

#### تسجيل الدخول
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

#### إضافة مستخدم جديد (Admin فقط)
```http
POST /api/auth/register
Authorization: Bearer {token}
Content-Type: application/json

{
  "username": "user1",
  "email": "user1@example.com",
  "password": "password123",
  "isAdmin": false
}
```

#### الحصول على بيانات المستخدم الحالي
```http
GET /api/auth/me
Authorization: Bearer {token}
```

### الفرق (Teams)

#### الحصول على جميع الفرق
```http
GET /api/teams
Authorization: Bearer {token}
```

#### إضافة فريق جديد
```http
POST /api/teams
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "فريق النجوم",
  "members": 8
}
```

#### تعديل فريق
```http
PUT /api/teams/:id
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "فريق النجوم المحدث",
  "members": 10
}
```

#### حذف فريق
```http
DELETE /api/teams/:id
Authorization: Bearer {token}
```

#### إضافة نقاط لفريق
```http
PATCH /api/teams/:id/points
Authorization: Bearer {token}
Content-Type: application/json

{
  "points": 100
}
```

### الإحصائيات (Stats)

#### الحصول على إحصائيات Dashboard
```http
GET /api/stats
Authorization: Bearer {token}
```

#### الحصول على لوحة المتصدرين
```http
GET /api/leaderboard
Authorization: Bearer {token}
```

### الأعضاء (Members)

#### الحصول على جميع الأعضاء
```http
GET /api/members?teamId={optional}
Authorization: Bearer {token}
```

#### إضافة عضو جديد
```http
POST /api/members
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "teamId": "60d5ec49f..."
}
```

---

## 🎨 التخصيص

### تغيير الألوان الرئيسية

في ملف `src/styles/global.css`:

```css
/* الألوان الأساسية */
--primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
--success-gradient: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
--danger-gradient: linear-gradient(135deg, #fc8181 0%, #e53e3e 100%);
```

### إضافة ميزات جديدة

1. أضف المسار في `backend/server.js`
2. أضف الـ API call في `frontend/src/services/api.js`
3. أضف الـ Component المطلوب
4. أضف الـ Styling في ملف CSS منفصل

---

## 🔒 الأمان

- **تشفير كلمات المرور**: باستخدام bcrypt
- **JWT Tokens**: صالحة لمدة 7 أيام
- **حماية المسارات**: Middleware للتحقق من الصلاحيات
- **CORS**: محدد للـ Frontend فقط
- **Validation**: التحقق من البيانات في Backend

---

## 🐛 حل المشاكل الشائعة

### مشكلة: MongoDB لا يعمل
```bash
# تحقق من تشغيل MongoDB
mongosh

# إذا لم يعمل، قم بتشغيله
mongod
```

### مشكلة: خطأ في الاتصال بـ API
```bash
# تأكد من أن Backend يعمل على المنفذ 5000
netstat -an | grep 5000

# تأكد من ملف .env في Frontend
cat frontend/.env
# يجب أن يحتوي على:
REACT_APP_API_URL=http://localhost:5000/api
```

### مشكلة: لا يمكن تسجيل الدخول
```bash
# تأكد من إنشاء حساب المدير
# قم بتشغيل createAdminUser() في server.js
```

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## 👨‍💻 المطور

تم تطويره بواسطة Claude AI

---

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى فتح issue على GitHub أو التواصل مباشرة.

---

## 🎉 شكر خاص

شكراً لاستخدام نظام التلعيب! نتمنى أن يكون مفيداً لك.

---

**ملاحظة**: هذا المشروع تعليمي ويمكن استخدامه كنقطة انطلاق لمشاريع أكبر.